admin:
  email: admin@example.com
  encrypted_password: <%= Devise::Encryptor.digest(User, AppConstants::DEFAULT_PASSWORD) %>
  role: admin

teacher:
  email: teacher@example.com
  encrypted_password: <%= Devise::Encryptor.digest(User, AppConstants::DEFAULT_PASSWORD) %>
  role: teacher
