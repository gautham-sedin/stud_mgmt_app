admin:
  email: admin@example.com
  encrypted_password: <%= Devise::Encryptor.digest(User, AppConstants::STUDENT_DEFAULT_PASSWORD) %>
  role: admin

teacher:
  email: teacher@example.com
  encrypted_password: <%= Devise::Encryptor.digest(User, AppConstants::STUDENT_DEFAULT_PASSWORD) %>
  role: teacher
